
  # UI

  This is a code bundle for UI. The original project is available at https://www.figma.com/design/zp6V2Xj06lOla82iqExfMT/UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  