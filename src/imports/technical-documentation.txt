Hey Figma AI, I need you to make several important updates to the smart home web app design. Please implement each of these changes precisely as described.

First, let's fix the Guide tab situation. In the current design, there's a Guide tab in the sidebar that contains detailed technical documentation about IoT device connection, push notification setup, and instructions for running the application. I need you to completely remove all of that content from the app interface. Instead, select all of that text content and export it as separate markdown or text files. Create a new folder called "technical-documentation" outside of the main app design and save three files there: iot-connection-guide.txt, push-notification-setup.txt, and how-to-run-the-app.txt. These are for our team's reference only.

Now rename the old Guide tab in the sidebar to "Help" and change its icon to a question mark inside a circle. Keep the same position in the navigation hierarchy. For the Help page content, create a simple, visually friendly guide with three steps. Use a clean card layout with numbered circles. Step one should say "Make sure your YoloBit device is powered on and connected to WiFi." Step two should say "Verify your device is publishing data to the correct Adafruit IO feeds." Step three should say "Your devices will automatically appear in the app once they're online." Add a refresh button at the bottom with a circular arrow icon that says "Check Connection Status." Below that, add a simple illustration showing a device icon with waves radiating out to represent connection.

Now I need you to completely overhaul all the popup modals throughout the admin sections. Go to Device Management and click the Add Device button. Currently, when this popup opens, the entire background turns black. I want you to change this behavior. The background should remain fully visible but with a semi-transparent overlay at 60 percent opacity black. The app content behind should be clearly visible but inactive, and no part of it should turn completely black. The popup itself should have a white background in light mode and a dark gray background in dark mode, with a subtle drop shadow and rounded corners of 12 pixels. Apply this same fix to the Add Room popup in Room Management and the Add User popup in User Management. All three popups should have consistent styling with the same overlay effect.

For the Room Management section, I need several specific changes. First, when I click on any room card from the main Room Management view, it should navigate to a detailed room view showing all devices in that room. At the top of this detailed view, keep the room name and icon, and add an "Add Device to This Room" button with a plus icon. When clicked, this button should trigger the exact same Add Device popup that appears in the main Device Management tab. The popup should include a dropdown that automatically preselects the current room, but allow changing it if needed.

Inside this room detail view, each device card should have a toggle switch if it's a controllable device like a light or fan. When I click this toggle to turn a device on or off, that change should be visually reflected everywhere in the app. Specifically, if I turn on a light in the kitchen from the room detail view, that same light should show as ON in the main dashboard's device summary, in the Control tab's device list, and in any other place that device appears. To make this visually clear in the prototype, when I click the toggle, all instances of that device across different screens should simultaneously update their state. Add a small animation to the toggle when clicked, and change the device card background to a light amber color with a soft glow effect when on for lights, or a cool blue tint for fans when on.

Add a prominent time display to the main dashboard. Place it in the top right corner of the main content area, just below the header and above the welcome banner. Make it a horizontal card with a light background in light mode and a dark background in dark mode, with subtle border radius of 8 pixels. Inside, show the current time in large, bold digits using a monospace font for the numbers. Below the time, show the current date in a slightly smaller, regular font. Next to the time, add a small icon of a clock. The time should automatically update visually every minute in the prototype to demonstrate dynamic behavior. For demo purposes, add a small dropdown arrow next to the time that when clicked shows a panel with different timezone options like New York, London, Tokyo, and Local, but keep the default as the system local timezone.

Finally, add a temperature simulation control specifically for testing automation rules. Locate this near the main temperature reading on the dashboard, probably inside the sensor summary card where temperature, humidity, and other sensors are displayed. Next to the current temperature value, add a small settings gear icon or a slider icon. When clicked, it expands a small control panel directly below the temperature reading. This panel should contain a slider control ranging from 15 degrees Celsius to 35 degrees Celsius, with the current value displayed in a bubble above the slider thumb. Add plus and minus buttons on either end of the slider for precise adjustment. Below the slider, add a toggle switch labeled "Simulation Mode" that when enabled overrides the real sensor data with the manually set value. When simulation mode is active, change the temperature display color to a light purple to indicate it's in test mode, and add a small pill badge next to it that says "Simulating." This control should persist its setting as I navigate between different tabs in the prototype.

Make sure all these changes maintain the existing light and dark mode themes. The new elements should respect the color variables we've established, using the proper background and foreground colors when switching between modes. Test that the popup overlay, the time display, the temperature simulation control, and all device toggles look correct in both themes.

Please implement all of these changes exactly as described and show me the updated prototype when complete.